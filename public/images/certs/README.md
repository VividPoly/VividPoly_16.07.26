# Certification logos

Drop official artwork here. Filenames must match:

- `iso.svg` (or `.png`)
- `iec.svg` (or `.png`)

Until files exist, the homepage shows circular ISO / IEC placeholder marks that swap automatically when these files are added.
