# VividPoly Next.js: copy to .env.local for local overrides (optional).
# No secrets are required for the static marketing site.

# Optional: override URL for responsive audit script (scripts/responsive-audit.mjs)
# VP_AUDIT_URL=http://localhost:3000
